<!-- pages/map/index.wxml -->
<view class="map-container">
  <!-- 原生地图组件 -->
  <map
    id="tencentMap"
    class="tencent-map"
    longitude="{{longitude}}"
    latitude="{{latitude}}"
    scale="{{scale}}"
    markers="{{markers}}"
    show-location="{{true}}"
    bindmarkertap="onMarkerTap"
    bindregionchange="onRegionChange"
    bindtap="onMapTap"
    enable-3D="{{false}}"
    show-compass="{{true}}"
    enable-overlooking="{{false}}"
    enable-zoom="{{true}}"
    enable-scroll="{{true}}"
    enable-rotate="{{false}}"
  >
    <!-- 自定义标注点 -->
    <cover-view slot="callout">
      <cover-view
        wx:for="{{markers}}"
        wx:key="id"
        class="custom-callout {{item.type}}"
        marker-id="{{item.id}}"
      >
        <cover-view class="callout-content">
          <cover-view class="callout-icon">{{item.type === 'helper' ? '🤝' : '🆘'}}</cover-view>
        </cover-view>
      </cover-view>
    </cover-view>
  </map>

  <!-- 顶部状态提示 -->
  <view class="status-bar">
    <view class="status-info">
      <text class="status-icon">📍</text>
      <text class="status-text">{{statusText}}</text>
    </view>
  </view>

  <!-- 右侧控制按钮 -->
  <view class="control-buttons">
    <!-- 定位按钮 -->
    <view class="control-btn" bindtap="moveToLocation">
      <text class="btn-icon">🎯</text>
    </view>
    <!-- 放大按钮 -->
    <view class="control-btn" bindtap="zoomIn">
      <text class="btn-icon">➕</text>
    </view>
    <!-- 缩小按钮 -->
    <view class="control-btn" bindtap="zoomOut">
      <text class="btn-icon">➖</text>
    </view>
  </view>

  <!-- 图例 -->
  <view class="legend">
    <view class="legend-item">
      <view class="legend-dot helper"></view>
      <text class="legend-text">可提供帮助</text>
    </view>
  </view>

  <!-- 底部信息面板 -->
  <view wx:if="{{selectedMarker}}" class="bottom-sheet {{showSheet ? 'show' : ''}}">
    <view class="sheet-header">
      <view class="sheet-info">
        <text class="sheet-type">🤝 附近的帮助者</text>
        <text class="sheet-distance">距离约 {{selectedMarker.distance}} 米</text>
        <text class="sheet-name">{{selectedMarker.nickName}}</text>
      </view>
      <view class="close-btn" bindtap="closeBottomSheet">
        <text>✕</text>
      </view>
    </view>

    <view class="sheet-resources">
      <text class="resources-title">可提供的资源（请选择）：</text>
      <view class="resources-list">
        <view
          wx:for="{{selectedMarker.resources}}"
          wx:key="index"
          class="resource-tag {{selectedResource === item ? 'selected' : ''}}"
          bindtap="selectResource"
          data-resource="{{item}}"
        >
          <text>{{item}}</text>
        </view>
      </view>
    </view>

    <view class="sheet-actions">
      <button class="action-btn primary" bindtap="contactPerson">
        请求资源
      </button>
      <button class="action-btn secondary" bindtap="navigateTo">
        导航
      </button>
    </view>
  </view>
</view>
